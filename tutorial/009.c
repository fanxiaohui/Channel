# include <stdio.h>
# include <stdlib.h>
 #include <string.h>


int main () {
    // char myString[] = "123456789and more";

    // int length = sizeof(myString)/sizeof(char);

    // printf("%s %d \n\n","array length is ",length);


    // char*s="GoldenGlobalView";
    // printf("%s has %d chars",s,strlen(s)); //计算数组长度
    // getchar();

    char cityName[]  = "San antonieo";
    // strlcpy(cityName,"El Pueblo del la Reina de Los ANgeles",sizeof(cityName));

    printf(" %s",cityName);
    return 0;
}